// crt.ts — constants cho CRT overlay (tách từ layout để AppShell dùng).
export const CRT_ENABLED_DEFAULT = true;
/** localStorage key cho legacy toggle (đã migrate sang lyc_prefs_v1.x). */
export const LEGACY_CRT_KEY = "vapor_crt_mode";
