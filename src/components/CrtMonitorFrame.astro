---
interface Props {
  title?: string;
  className?: string;
}

const {
  title = "SONY TRINITRON // MULTISCAN 17SF",
  className = ""
} = Astro.props;
---

<div class={`bg-win-gray p-2.5 sm:p-3.5 rounded-lg border-4 border-win-light crt-bezel-shadow font-retro text-black w-full max-w-full overflow-hidden flex flex-col justify-between ${className}`}>
  <!-- Brand Badge & Speaker Grills -->
  <div class="flex justify-between items-center mb-2 px-0.5 gap-2 min-w-0">
    <div class="flex items-center gap-1.5 min-w-0 flex-1">
      <span class="w-2 h-2 sm:w-2.5 sm:h-2.5 bg-black rounded-full inline-block border border-win-dark shrink-0"></span>
      <span class="text-[10px] sm:text-[10px] font-bold font-mono tracking-wide text-win-darkest uppercase truncate" title={title}>
        {title}
      </span>
    </div>
    
    <div class="flex gap-1 shrink-0">
      <span class="w-3 sm:w-4 h-1 bg-win-dark rounded-full"></span>
      <span class="w-3 sm:w-4 h-1 bg-win-dark rounded-full"></span>
      <span class="w-3 sm:w-4 h-1 bg-win-dark rounded-full"></span>
    </div>
  </div>

  <!-- Screen Bezel & Tube -->
  <div class="bg-black p-1.5 sm:p-2.5 rounded-md border-4 border-win-darkest crt-tube-deep relative overflow-hidden flex-1">
    <!-- Curved Glass Vignette & Scanlines -->
    <div class="relative rounded-sm overflow-hidden z-10 w-full h-full">
      <slot />
    </div>

    <!-- Scanline effect across screen -->
    <div class="absolute inset-0 pointer-events-none crt-scanlines-repeating z-20"></div>
    <div class="absolute inset-0 pointer-events-none crt-vignette-glass z-20"></div>
  </div>

  <!-- Monitor Control Panel & Power LED -->
  <div class="flex flex-wrap justify-between items-center mt-2.5 pt-2 border-t border-win-dark/40 px-0.5 gap-y-1.5 gap-x-2">
    <!-- Left: Power LED -->
    <div class="flex items-center gap-1.5 shrink-0">
      <span class="w-2 h-2 rounded-full bg-vapor-green glow-green animate-pulse shrink-0"></span>
      <span class="text-[10px] font-mono text-win-darkest font-bold">POWER</span>
      <span class="text-[10px] font-mono text-win-darkest hidden 2xl:inline opacity-70">● 75Hz</span>
    </div>

    <!-- Right: Responsive Control Buttons -->
    <div class="flex items-center gap-1 shrink-0 ml-auto">
      <button
        type="button"
        class="win95-btn win95-mini-btn text-[10px] sm:text-[10px] font-mono py-0 px-1.5 leading-none shrink-0 active:translate-y-0.5"
        title="Degauss CRT Tube"
      >
        DEGAUSS
      </button>
      <button
        type="button"
        class="win95-btn win95-mini-btn text-[10px] sm:text-[10px] font-mono py-0 px-1.5 leading-none shrink-0 active:translate-y-0.5"
        title="Adjust Contrast / Brightness"
      >
        ADJ
      </button>
      <button
        type="button"
        class="win95-btn win95-mini-btn text-[10px] sm:text-[10px] font-mono font-bold text-vapor-pink py-0 px-1.5 leading-none shrink-0 active:translate-y-0.5"
        title="Monitor OSD Menu"
      >
        MENU
      </button>
    </div>
  </div>
</div>
